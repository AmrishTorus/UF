apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ct009-tob002-uf-v1-ingress
  namespace: ct009-tob002-uf-v1
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tgadev.toruslowcode.com
      secretName: ct009-tob002-uf-v1-tls
  rules:
    - host: tgadev.toruslowcode.com
      http:
        paths:
          - path: /ct009/tob001/tob002/v1
            pathType: Prefix
            backend:
              service:
                name: ct009-tob002-uf-generator
                port:
                  number: 443